#include "Mesh.h"

#include <stdlib.h>
#include <glad/glad.h>

Mesh *create_mesh_from_chunk(Chunk *chunk)
{
    Mesh *mesh = malloc(sizeof(Mesh));



    return mesh;
}

void free_mesh(Mesh *mesh)
{
    
}